# Accessibility Guidelines (CLI-first)

- Offer **JSON output** for machine readability and screen-reader friendly parsing.
- Avoid mandatory colors; default to **no color** and document environment toggles.
- Provide **concise errors** with **stable exit codes** (see `docs/EXITCODES.md`).
- Keep man pages and README navigable with headings and short paragraphs.
