# C API Overview
See `include/neonstore/capi.h` for types and functions. The C API is stable across minor versions. Link with the shared or static library and include headers from the installed prefix.
