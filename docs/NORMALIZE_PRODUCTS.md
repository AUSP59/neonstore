# normalize (Product Name Normalization)

```bash
neonstore normalize --trim --collapse-spaces --lower   # example
```
Trims edges, collapses internal whitespace, and adjusts case. Use `--dry-run` to preview.
