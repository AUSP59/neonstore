# Versioning

- The project reports its version via `neonstore version` (JSON/text). Current: **2.38.0**.
- CMake defines `NEONSTORE_VERSION` and the binary embeds `NEONSTORE_VERSION_STR`.
- Releases follow semantic versioning documented in `CHANGELOG.md`.
