# Accessibility Guidelines

- All CLI outputs offer a `--output json` mode for machine readability.
- Error messages are concise and actionable with stable exit codes.
- No color-coding reliance by default; plain text is used everywhere.
- Docs and examples avoid jargon; provide short and long forms for commands.
