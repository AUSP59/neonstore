# Config

Simple key-value configuration stored in `.neonstore/config.ini`:
```bash
neonstore config set api.timeout 30
neonstore config get api.timeout
neonstore config list
neonstore config unset api.timeout
```
