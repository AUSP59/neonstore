# vdiff (Verbose Dataset Diff)

```bash
neonstore vdiff --dir2 other_dir [--dir PATH] [--output json|text]
```
Prints counts **and** explicit id lists for products added/removed and changed, plus order/item deltas.
