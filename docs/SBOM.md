# SBOM (Simplified)

`neonstore sbom generate` walks a directory tree and emits a file inventory (path, size, mtime)
in JSON or text. For cryptographic digests, rely on file sidecars (SHA-256) and dataset seals.
