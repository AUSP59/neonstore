# Lint

- `lint refs` → reports order items whose `product_id` is missing in `products.csv` (exit 2 on violations).
- `lint csv` → checks non-negative prices/stocks/unit prices and positive quantities (exit 2 on violations).
