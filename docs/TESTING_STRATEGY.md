# Testing Strategy
- Unit tests (assert-based) via CTest.
- Integration tests via CLI invocations in future work.
- Fuzz targets for CSV import with libFuzzer (optional).
- Sanitizers in CI; coverage target initial 80% lines.
